# Local development can keep this empty because Vite proxies /api to localhost:5000.
# On Vercel, set this to your Render backend URL, for example:
# VITE_API_URL=https://club-mall-api.onrender.com
VITE_API_URL=
